# 圆柱镜面几何参数与摆放位置的确定：变量常设策略与推导

## 1. 问题的优化特征

在由镜面图案反求纸面图案的实际设计任务中，圆柱镜面的半径 $R$、高度 $H$ 及其底面中心在 A4 纸上的坐标 $(X_0,Y_0)$ 均是待定参数。观察者位置 $(L,H_e)$ 和镜面图案所占的角范围 $\alpha$ 与高度区间 $[z_1,z_2]$ 通常由外部需求或参照图给定，可视为**设计常数**。然而，由于约束条件之间存在耦合，直接同时求解所有未知量将导致多变量非线性规划问题。工程上更高效且可靠的方法，是采用**逐次固定部分参数、分阶段求解**的策略，将多维优化转化为一系列单变量或双变量判定问题。

本文阐明在利用极坐标正向映射确定 $R$、$H$ 和 $(X_0,Y_0)$ 的过程中，哪些变量被假定为常数，其合理性何在，以及如何通过有序的计算步骤得到可行且实用的解。

---

## 2. 参数角色分类

| 类别 | 符号 | 含义 | 确定方式 |
|------|------|------|----------|
| **固定常数** | $L$, $H_e$ | 观察者眼睛的水平距离与离纸面高度 | 由使用场景预设（如阅读距离 $300\text{ mm}$，眼高 $200\text{ mm}$） |
| **半固定常数** | $\alpha$, $\Delta z$, $z_1$, $z_2$ | 镜面图案的角跨度与高度区间 | 由目标图案的视觉比例决定；初次估计后可微调 |
| **阶段性常数** | $H$ | 圆柱物理高度 | 在第一阶段根据 $z_2$ 预设，第二阶段视情况微调 |
| **核心求解变量** | $R$ | 圆柱半径 | 通过纸面边界容纳性条件选取 |
| **核心求解变量** | $(X_0,Y_0)$ | 圆柱底面中心在 A4 上的坐标 | 在 $R$ 确定后，由图案居中原则计算并校验 |

这种分类的本质是将自由度数控制在每一步计算可处理的范围内，同时保证设计结果的实用性与物理可行性。

---

## 3. 常数假设的依据

### 3.1 观察者位置 $(L, H_e)$
观察距离 $L$ 和视线高度 $H_e$ 属于**使用条件**，而非自由设计变量。例如，桌面展示品的典型观察距离为 $300\sim400\text{ mm}$，视线高度约 $200\text{ mm}$。一旦选定目标场景，这两个量即可固定，不再作为优化变量。若最终纸面图案超出 A4 边界，可优先调整圆柱参数，而非观察位置。

### 3.2 镜面图案定义域 $\alpha$, $[z_1,z_2]$
镜面图案的期望视觉内容决定了其在柱面上的“物理占据范围”。$\alpha$ 和高度区间可由参考图像通过**视角映射**初步确定（见前述“镜面参数化”方法），并在尺寸优化过程中作为**不可随意变更的设计输入**。仅在极端的边界冲突下，才考虑对 $\alpha$ 或 $z_2$ 进行有限缩放（相当于对原图案做轻微裁剪或垂向压缩）。

### 3.3 圆柱高度 $H$
圆柱高度须满足 $H \ge z_2$，以确保整个镜面图案能被镜面容纳。实践中，通常直接选取 $H = z_2 + \delta$，其中 $\delta$ 为安全余量（例如 $10\text{ mm}$）。这样 $H$ 在首个计算阶段被固定为常数，后续验证时可酌情调整，但一般无需反复迭代。

---

## 4. 公式推导与约束转化

### 4.1 正向映射回顾
纸面极坐标由镜面柱坐标 $(\theta,z)$ 通过下式确定（$R$ 隐含在式中）：
$$
\begin{aligned}
\rho(\theta,z) &= \sqrt{\alpha^2 R^2 + \beta^2 L^2 + 2\alpha\beta R L\cos\theta},\\[4pt]
\phi(\theta,z) &= \operatorname{atan2}\!\big(\alpha R\sin\theta + \beta L\sin2\theta,\; \alpha R\cos\theta + \beta L\cos2\theta\big),
\end{aligned}
$$
其中
$$
\alpha(z) = \frac{H_e-2z}{H_e-z},\quad \beta(z) = \frac{z}{H_e-z}.
$$

### 4.2 纸面区域边界与 A4 容纳条件
取镜面图案的四个代表性角点：
$$
\begin{aligned}
A(\theta{=}-\alpha/2, z{=}z_1),\quad 
B(\theta{=}+\alpha/2, z{=}z_1),\\
C(\theta{=}+\alpha/2, z{=}z_2),\quad 
D(\theta{=}-\alpha/2, z{=}z_2).
\end{aligned}
$$
利用正向映射求出它们对应的纸面极坐标，记为 $(\rho_i,\phi_i)$。纸面图案的跨度为：
$$
\rho_{\min} = \min_i\rho_i,\;\; \rho_{\max} = \max_i\rho_i,\qquad
\phi_{\min} = \min_i\phi_i,\;\; \phi_{\max} = \max_i\phi_i.
$$

纸面上实际直角坐标为（以 A4 坐标表示）：
$$
X = X_0 + \rho\cos\phi,\qquad Y = Y_0 + \rho\sin\phi.
$$
A4 幅面（纵向放置）约束：
$$
0 \le X \le 210,\qquad 0 \le Y \le 297\quad\text{（单位：mm）}.
$$

此外，镜面图案必须完全位于圆柱可见反射区域内，这给出半径的下限约束：
$$
\sin\frac{\alpha}{2} \le \frac{R}{\sqrt{L^2 + H_e^2}}\quad\text{或更精确的视线遮挡条件}.
$$
在典型参数下，可视作 $R \ge L\sin(\alpha/2)$ 的近似需求，但更严格的约束将在计算中通过检查视线与柱面交点的存在性自然满足。

### 4.3 由约束反求 $R$ 的单变量扫描
将 $H$ 固定为 $z_2+\delta$，并将 $(L,H_e,\alpha,z_1,z_2)$ 视为常数。纸面边界点坐标 $(X_i,Y_i)$ 可表达为 $R$ 与 $(X_0,Y_0)$ 的函数。

1. **临时定位**：先假定一个虚拟的中心位置，例如令镜面中心点 $(\theta=0,z=z_c=(z_1+z_2)/2)$ 映射到纸面极坐标 $(\rho_c,\phi_c)$，并强制该点位于 A4 中央：  
   $X_0^{\text{temp}} = 105 - \rho_c\cos\phi_c$，$Y_0^{\text{temp}} = 148.5 - \rho_c\sin\phi_c$。此时 $(X_0^{\text{temp}},Y_0^{\text{temp}})$ 也是 $R$ 的函数。

2. **单变量可行性检查**：对一个候选 $R$，计算所有边界点的 A4 坐标，并检查是否满足 $[0,210]\times[0,297]$。通过二分查找或扫描，可确定使图案完全容纳的 $R$ 的允许区间 $[R_{\min},R_{\max}]$。

3. **优选 $R$**：在允许区间内，选取使得图案在纸面上所占面积（或径向跨度）较大的 $R$，以获得更高印刷分辨率和制作容忍度。通常直接取 $R_{\max}$ 作为最优半径。

在上述过程中，**只有 $R$ 是扫描变量**，$H$ 和 $(X_0,Y_0)$ 的临时值均由 $R$ 确定。一旦选定 $R$，$H$ 和 $(X_0,Y_0)$ 也随之确定，并可在下一步进行独立微调。

### 4.4 摆放位置 $(X_0,Y_0)$ 的精细化
选定 $R$ 后，$H$ 保持常数。将 $R$ 代入正向映射，重新计算图案所有边界点的坐标。若使用临时居中位置已满足全部约束且留有余量，则可直接采用；若局部超界，则进行平移：
$$
\begin{aligned}
X_0 &\leftarrow X_0 - \frac{1}{2}\big(\min X_i + \max X_i - 210\big),\\
Y_0 &\leftarrow Y_0 - \frac{1}{2}\big(\min Y_i + \max Y_i - 297\big),
\end{aligned}
$$
直至所有点均在范围内并保持适当边距。此过程仅对 $(X_0,Y_0)$ 做简单校正，无需重新计算 $R$。

### 4.5 是否需要多次常设不同变量？
原则上，**一轮固定 $L,H_e,H$ 并扫描 $R$ 后即可获得可行解**，无需反复将不同变量设为常数进行多轮迭代。但在以下两种情况下可能需进行第二遍调整：

- 若扫描发现 $R$ 无可行区间（即无论 $R$ 取何值，图案总超出 A4），则必须放松某些固定常数，例如增大 $L$（改变观察距离）、减小 $\alpha$（裁切镜面图案）或适当缩小 $z_2$。此时会将原先的常数临时提升为变量进行有限范围的搜索。
- 若设计要求同时兼顾纸面图案的美观布局（如希望在特定位置留白），可能在选定 $R$ 后微调 $H$ 或 $z_2$，但这一般属于艺术加工范畴，而非数学必须。

因此，**标准的求解路径是仅需一次常数设定**：观察参数和图案范围为常数，圆柱高度为预估常数，半径 $R$ 为主要求解对象，中心位置跟随确定。这种设计流程稳定且易于程序化。

---

## 5. 算法步骤总结

**输入**：$L$, $H_e$, $\alpha$, $z_1$, $z_2$（均已固定为常数）  
**输出**：$R$, $H$, $(X_0,Y_0)$

1. **预设圆柱高度**：$H = z_2 + 10\text{ mm}$。
2. **扫描半径 $R$**：
   - 设置 $R$ 的搜索范围 $[R_{\text{low}}, R_{\text{high}}]$（如下界由机械可行性给出，上界由 $L/2$ 等粗略限定）。
   - 对每一 $R$，计算介面临时中心位置 $X_0^{\text{temp}}, Y_0^{\text{temp}}$，并检查所有边界角点是否落入 A4。
   - 记录可通过检查的 $R$ 区间，并选取其中的最大值。
3. **确定中心位置**：使用选定 $R$，计算精细的纸面区域边界，按平移校正公式定出最终的 $(X_0,Y_0)$，确保完全在 A4 内并保留边距。
4. **验证**：检查可见性约束，并模拟反射结果。如不满足，放宽常数限制后返回步骤 1。

---

## 6. 结语

本方案将多参数联合优化问题解耦为“常量预设—单变量扫描—位置微调”的三阶段流程，物理意义清晰、计算简单。常数的设定并非随意，而是基于实际使用场景的不变性和设计输入的保护优先级，确保最终的圆柱尺寸与摆放位置切实可用，且留有余地供艺术后期处理。
